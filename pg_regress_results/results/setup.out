-- pg_regress setup: clean install of the extension
-- Drop and recreate to wipe all prior test data (pgrx reuses the database).
DROP EXTENSION IF EXISTS pg_ripple CASCADE;
CREATE EXTENSION pg_ripple;
