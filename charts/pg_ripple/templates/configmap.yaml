apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pg-ripple.fullname" . }}-config
  labels:
    {{- include "pg-ripple.labels" . | nindent 4 }}
data:
  pg_ripple.conf: |
    {{- range $k, $v := .Values.postgres.extraConf }}
    {{ $k }} = {{ $v | quote }}
    {{- end }}
