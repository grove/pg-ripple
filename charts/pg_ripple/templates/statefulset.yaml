apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "pg-ripple.fullname" . }}
  labels:
    {{- include "pg-ripple.labels" . | nindent 4 }}
spec:
  serviceName: {{ include "pg-ripple.fullname" . }}-headless
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "pg-ripple.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "pg-ripple.selectorLabels" . | nindent 8 }}
      annotations:
        {{- toYaml .Values.podAnnotations | nindent 8 }}
    spec:
      {{- if and .Values.http.allowUnauthenticated (ne .Values.http.mode "development") }}
      {{- fail "http.allowUnauthenticated requires http.mode=development" }}
      {{- end }}
      serviceAccountName: {{ include "pg-ripple.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: postgres
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: postgres
              containerPort: 5432
              protocol: TCP
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "pg-ripple.fullname" . }}-secret
                  key: postgres-password
            - name: POSTGRES_DB
              value: {{ .Values.postgres.database | quote }}
            {{- if .Values.ripple.llm.apiKeySecret }}
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.ripple.llm.apiKeySecret }}
                  key: {{ .Values.ripple.llm.apiKeySecretKey }}
            {{- end }}
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
            - name: postgres-config
              mountPath: /etc/postgresql/conf.d
        {{- if .Values.http.enabled }}
        - name: sparql-http
          securityContext:
            {{- toYaml .Values.http.securityContext | nindent 12 }}
          image: "{{ .Values.http.image.repository | default .Values.image.repository }}:{{ .Values.http.image.tag | default .Values.image.tag }}"
          imagePullPolicy: {{ .Values.http.image.pullPolicy | default .Values.image.pullPolicy }}
          command: ["/usr/local/bin/pg_ripple_http"]
          ports:
            - name: http
              containerPort: 7878
              protocol: TCP
          env:
            - name: PG_RIPPLE_HTTP_MODE
              value: {{ .Values.http.mode | quote }}
            - name: PG_RIPPLE_HTTP_BIND
              value: "0.0.0.0:7878"
            - name: PG_RIPPLE_HTTP_PG_URL
              value: "postgresql://postgres@localhost/{{ .Values.postgres.database }}"
            - name: PG_RIPPLE_HTTP_PG_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "pg-ripple.fullname" . }}-secret
                  key: postgres-password
            {{- if .Values.http.writeTokenSecret.name }}
            - name: PG_RIPPLE_HTTP_WRITE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.http.writeTokenSecret.name }}
                  key: {{ .Values.http.writeTokenSecret.key | quote }}
            {{- end }}
            {{- if .Values.http.adminTokenSecret.name }}
            - name: PG_RIPPLE_HTTP_ADMIN_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.http.adminTokenSecret.name }}
                  key: {{ .Values.http.adminTokenSecret.key | quote }}
            {{- end }}
            {{- if .Values.http.metricsTokenSecret.name }}
            - name: PG_RIPPLE_HTTP_METRICS_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.http.metricsTokenSecret.name }}
                  key: {{ .Values.http.metricsTokenSecret.key | quote }}
            {{- end }}
            - name: PG_RIPPLE_HTTP_PORT
              value: "7878"
            - name: PG_RIPPLE_HTTP_RATE_LIMIT
              value: {{ .Values.http.rateLimit | quote }}
            - name: PG_RIPPLE_HTTP_CORS_ORIGINS
              value: {{ .Values.http.corsOrigins | quote }}
            {{- if .Values.http.allowUnauthenticated }}
            - name: PG_RIPPLE_HTTP_ALLOW_UNAUTHENTICATED
              value: "1"
            {{- else }}
            - name: PG_RIPPLE_HTTP_AUTH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ required "http.authTokenSecret.name is required unless http.allowUnauthenticated is true" .Values.http.authTokenSecret.name }}
                  key: {{ .Values.http.authTokenSecret.key | quote }}
            {{- end }}
            {{- range $k, $v := .Values.http.env }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
        {{- end }}
      volumes:
        - name: postgres-config
          configMap:
            name: {{ include "pg-ripple.fullname" . }}-config
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: [{{ .Values.persistence.accessMode | quote }}]
        {{- if .Values.persistence.storageClass }}
        storageClassName: {{ .Values.persistence.storageClass | quote }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.persistence.size | quote }}
