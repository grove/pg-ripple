# v0.72.0 Full Technical Details - Architecture, Protocol Hardening, and Conformance Gates

## Goals

| ID | Goal | Priority |
|----|------|----------|
| XACT-01 | Register `SubXactCallback` + `XactCallback` to make mutation journal safe under `SAVEPOINT` / `ROLLBACK` | Required |
| CACHE-01 | Document plan cache contract; add role-isolation regression test | Required |
| MOD-01 | Reduce `src/lib.rs`, `src/storage/mod.rs`, and `pg_ripple_http/src/routing.rs` below 500 lines each | Required |
| PROPTEST-01 | Add `proptest` suite for `ConstructTemplate` / `apply_construct_template` | Required |
| FUZZ-02 | Add `fuzz/fuzz_targets/sparql_update.rs` for SPARQL Update grammar; seed corpus | Required |
| CONF-01 | Promote W3C SPARQL 1.1 smoke subset and BSBM regression gate to required (blocking) CI | Required |
| BENCH-03 | Backfill `benchmarks/merge_throughput_history.csv` for v0.67.0–v0.69.0; confirm weekly workflow commits rows | Required |
| FLIGHT-NONCE-01 | Arrow Flight ticket nonce-replay protection via server-side LRU cache | Required |
| CWB-DATALOG-01 | Confirm Datalog-derived triples trigger downstream CONSTRUCT rules; add integration test | Required |
| OBS-02 | Expose extension `streaming_metrics()` via a `/metrics/extension` route in `pg_ripple_http` | Required |
| GUC-DOC-01 | `docs/src/reference/guc-reference.md`: cross-reference `arrow_batch_size`, `export_batch_size`, `vp_promotion_batch_size` | Required |
| CITUS-URL-01 | Add unit tests for `is_citus_worker_endpoint()` / `extract_url_host()` edge cases | Required |
| JSONLD-NODE-01 | Add `pg_ripple.export_jsonld_node(frame, subject_id, strip)` to eliminate CDC bridge trigger boilerplate | Required |
| BUG-JSONLD-LIST-01 | Fix `@container: @list` support in inbound context parser and outbound framing embedder | Required |

---

## Release theme

v0.72.0 is an architecture depth and protocol hardening release. The headlining item is XACT-01: registering a proper PostgreSQL transaction/subtransaction callback so the mutation journal does not fire CWB for triples whose storage writes were subsequently rolled back. The remaining items harden conformance, observability, and protocol correctness.

---

## Deliverables

### XACT-01 — Mutation journal SAVEPOINT safety

**Root cause**: [src/storage/mutation_journal.rs](../src/storage/mutation_journal.rs) uses `thread_local! RefCell<Vec<JournalEntry>>` cleared by `flush()`. If a subtransaction is rolled back, already-flushed CWB invocations have written derived triples against now-absent source rows.

**Fix**:

1. In `_PG_init` (or `mutation_journal::init()` called from `_PG_init`), register:
   ```rust
   pgrx::pg_sys::RegisterSubXactCallback(Some(sub_xact_callback), std::ptr::null_mut());
   pgrx::pg_sys::RegisterXactCallback(Some(xact_callback), std::ptr::null_mut());
   ```
2. `sub_xact_callback`: on `PG_SUBXACT_EVENT_ABORT_SUB`, clear the thread-local journal.
3. `xact_callback`: on `XACT_EVENT_PRE_COMMIT`, call `mutation_journal::flush()` (the deferred flush introduced in v0.70.0 FLUSH-01); on `XACT_EVENT_ABORT`, clear the journal.
4. Remove the `RegisterXactCallback` workaround from v0.70.0 FLUSH-01 if it uses a simpler mechanism; replace with the canonical callback approach.

**Test**: Add `tests/pg_regress/sql/cwb_savepoint_rollback.sql`:
- Register a CONSTRUCT rule.
- `BEGIN; SAVEPOINT s1; INSERT triple into source graph; ROLLBACK TO SAVEPOINT s1; COMMIT;`
- Assert derived target graph is empty (no phantom derived triples from the rolled-back source).

### CACHE-01 — Plan cache contract documentation and test

**Root cause**: Assessment 10 HF-6 noted that the plan cache key omits graph bindings and current role.

**Work items**:

1. Add `docs/src/reference/plan-cache.md` documenting:
   - Cache key construction: SPARQL text → canonical algebra (via spargebra Display) + `max_depth` + `bgp_reorder` GUC values.
   - What is **not** in the key: graph bindings, current role, RLS-relevant context.
   - Why this is safe: PostgreSQL enforces RLS at executor level; the plan cannot be incorrect, only slower than a role-specialized plan would be.
   - How to invalidate: `SELECT pg_ripple.plan_cache_reset()`.
2. Add `tests/pg_regress/sql/plan_cache_rls.sql`:
   - Two roles: `alice` (access to graph A) and `bob` (access to graph B only).
   - Same SPARQL text run as alice, then as bob.
   - Assert alice sees graph A triples; bob sees graph B triples (not alice's).

### MOD-01 — Continue module split

**Root cause**: `src/lib.rs` (~2,262 lines), `src/storage/mod.rs` (~2,104 lines), and `pg_ripple_http/src/routing.rs` (~1,200 lines) still exceed 500 lines.

**Work items**:

1. Extract GUC registration from `src/lib.rs` into `src/gucs/registration.rs`.
2. Extract `_PG_init` shared memory initialization from `src/lib.rs` into `src/shmem_init.rs`.
3. Extract dictionary I/O from `src/storage/mod.rs` into `src/storage/dictionary_io.rs`.
4. Extract VP-rare I/O from `src/storage/mod.rs` into `src/storage/vp_rare_io.rs`.
5. Extract SPARQL-endpoint handler functions from `pg_ripple_http/src/routing.rs` into `pg_ripple_http/src/sparql_handlers.rs`.
6. Confirm no pg_regress regressions.

**Exit criterion**: All three files ≤ 500 lines.

### PROPTEST-01 — ConstructTemplate property tests

**Root cause**: v0.68.0 added `prepare_construct()` / `apply_construct_template()` with no property tests.

**Work items**:

1. Add `src/sparql/proptest_construct.rs` (or `tests/proptest/construct_template.rs`).
2. Generator: random binding sets for a fixed CONSTRUCT template.
3. Property: `apply_construct_template(template, bindings)` produces the same set of quads as evaluating the CONSTRUCT naively over the same bindings.
4. Run via `cargo test --features pg18 -- proptest`.

### FUZZ-02 — SPARQL Update fuzz target

**Root cause**: `fuzz/fuzz_targets/sparql_parser.rs` targets `parse_query`; SPARQL Update uses `parse_update`.

**Work items**:

1. Create `fuzz/fuzz_targets/sparql_update.rs`:
   ```rust
   fuzz_target!(|data: &[u8]| {
       if let Ok(s) = std::str::from_utf8(data) {
           let _ = spargebra::SparqlParser::new().parse_update(s);
       }
   });
   ```
2. Seed corpus with valid SPARQL Update fragments (INSERT DATA, DELETE DATA, DELETE/INSERT WHERE, COPY, MOVE, ADD, CLEAR, DROP).
3. Add to `fuzz.yml` workflow.

**Exit criterion**: 13 fuzz targets in the workflow; SPARQL Update target included.

### CONF-01 — Conformance gate promotion

**Root cause**: W3C SPARQL 1.1 full suite and BSBM are informational (`continue-on-error: true`) in `ci.yml`.

**Work items**:

1. Identify the smallest blocking-safe subset of the W3C SPARQL 1.1 suite: all tests in `sparql11/syntax/`, `sparql11/basic/`, `sparql11/optional/`, `sparql11/graph/`. Set this subset as a required step.
2. Promote BSBM regression gate to required: add `exit 1` when BSBM query execution time exceeds `1.5 ×` the baseline.
3. Leave the full W3C suite (3,000+ tests) and WatDiv as informational with a tracked pass-rate trend.

### BENCH-03 — Benchmark history backfill

**Root cause**: [benchmarks/merge_throughput_history.csv](../benchmarks/merge_throughput_history.csv) has only a v0.59.0 row.

**Work items**:

1. Run `performance_trend.yml` against v0.67.0, v0.68.0, v0.69.0 git tags and append rows.
2. Confirm the weekly scheduled workflow in `performance_trend.yml` has a step that commits new rows back to `main` via a bot token.

### FLIGHT-NONCE-01 — Arrow Flight replay protection

**Root cause**: Assessment 10 MF-19 noted that a signed ticket can be replayed within its expiry window.

**Fix**:

1. In `pg_ripple_http/src/arrow_encode.rs`, maintain a `DashMap<String, Instant>` (nonce → accepted-at) keyed by the ticket `nonce` field.
2. On ticket validation, check if `nonce` is in the map. If present and within the expiry window, reject with HTTP 401 and `arrow_ticket_rejections` metric increment.
3. Evict entries where `accepted-at + expiry_secs` has elapsed (lazy eviction on insertion).
4. Cap map size at `max_nonce_cache` entries (configurable, default 10,000).

**Test**: Add HTTP integration test that presents the same ticket twice within the expiry window and asserts the second response is 401.

### CWB-DATALOG-01 — Datalog → CWB interaction

**Root cause**: Assessment 10 MF-17 noted it is unknown whether Datalog-derived triples (written by the lattice engine) trigger downstream CONSTRUCT rules.

**Work items**:

1. Trace the Datalog write path: confirm it routes through `storage::insert_triple_by_ids` (which now calls `mutation_journal::record_write`).
2. Add `tests/pg_regress/sql/datalog_cwb_interaction.sql`:
   - Register a CONSTRUCT rule that fires when triples with predicate `:derived` appear.
   - Register a Datalog rule that produces `:derived` triples from input triples.
   - Insert base triples; assert CONSTRUCT target graph contains the expected derived triples.

### OBS-02 — Streaming metrics via HTTP

**Root cause**: Assessment 10 MF-13 noted extension `streaming_metrics()` counters are not exposed via the HTTP companion's `/metrics` route.

**Fix**:

1. Add `GET /metrics/extension` route in `pg_ripple_http/src/routing.rs` that calls `pg_ripple.streaming_metrics()` via SPI and returns the JSONB.
2. Or, expose the same data via the Prometheus `/metrics` endpoint as `pg_ripple_*` gauge metrics.
3. Document in `docs/src/operations/observability.md`.

### GUC-DOC-01 — Batch-size GUC cross-reference

Create `docs/src/reference/guc-reference.md` with a table covering at minimum:

| GUC | Type | Default | Purpose |
|-----|------|---------|---------|
| `pg_ripple.arrow_batch_size` | INT | 1000 | Arrow IPC export record batch size |
| `pg_ripple.export_batch_size` | INT | 100 | SPARQL cursor page size in rows |
| `pg_ripple.vp_promotion_batch_size` | INT | 10000 | VP promotion copy-phase batch size |

Explain interactions: `export_batch_size` controls cursor page size, `arrow_batch_size` controls Arrow IPC batching, and the two operate independently.

### CITUS-URL-01 — URL parsing edge cases

**Root cause**: Assessment 10 MF-18. Add unit tests for `is_citus_worker_endpoint()` / `extract_url_host()`.

Add `tests/pg_regress/sql/citus_url_parsing.sql` with inputs:
- `http://[::1]:5432/db` (IPv6)
- `http://xn--bcher-kva.example.com/db` (IDN)
- `http://host:5432` (port-only, no path)
- `not-a-url` (malformed — should return false, not panic)
- `http://worker1.internal/db` (normal case)

### JSONLD-NODE-01 — `export_jsonld_node()` convenience function

**Root cause**: UX-01. CDC bridge triggers that use `export_jsonld_framed()` to produce plain JSON currently require three repeated steps at every call site:
1. Append `@id` to the static frame: `frame || jsonb_build_object('@id', decode_id(NEW.s))`
2. Unwrap the node: `framed -> '@graph' -> 0`
3. Strip JSON-LD keywords: `- '@type' - '@id'`

This boilerplate is error-prone and obscures intent. The three steps are always performed together; the engine already has all the information needed to do them internally.

**Signature**:

```sql
CREATE OR REPLACE FUNCTION pg_ripple.export_jsonld_node(
    frame      JSONB,
    subject_id BIGINT,
    strip      TEXT[]  DEFAULT ARRAY['@type', '@id']
) RETURNS JSONB
```

**Semantics**:

1. Look up the IRI for `subject_id` in `_pg_ripple.dictionary`. Raise an error if not found.
2. Append `{"@id": "<iri>"}` to `frame` and call `export_jsonld_framed(frame => ...)` internally.
3. Extract `result -> '@graph' -> 0`. If `NULL` (no matching triples), return `NULL`.
4. Remove each key in `strip` from the node using the JSONB `-` operator.
5. Return the resulting JSONB object.

**Parameters**:

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `frame` | `JSONB` | — | Static frame template. Must contain `@context` and at least one property slot (`{}`). Must **not** contain `@id`; `subject_id` provides it. |
| `subject_id` | `BIGINT` | — | Dictionary ID of the subject node. In a VP table trigger, pass `NEW.s` directly. |
| `strip` | `TEXT[]` | `ARRAY['@type','@id']` | JSON-LD keyword keys to remove from the output node. Pass `ARRAY[]::TEXT[]` to keep all keywords (returns a full JSON-LD node object). |

**Typical trigger usage**:

```sql
CREATE OR REPLACE FUNCTION bridge_alert_to_outbox()
RETURNS TRIGGER LANGUAGE plpgsql AS $$
BEGIN
    INSERT INTO enriched_events (event_type, payload)
    VALUES (
        'alert',
        pg_ripple.export_jsonld_node(
            frame => '{
                "@context": {
                    "device": "https://saref.etsi.org/core/measurementMadeBy",
                    "temp":   "https://saref.etsi.org/core/hasValue",
                    "unit":   "https://qudt.org/schema/qudt/unit",
                    "ts":     "https://saref.etsi.org/core/hasTimestamp",
                    "alert":  "https://example.org/alertType"
                },
                "@type":  "https://saref.etsi.org/core/Measurement",
                "device": {}, "temp": {}, "unit": {}, "ts": {}, "alert": {}
            }'::jsonb,
            subject_id => NEW.s
        )
    );
    RETURN NEW;
END;
$$;
```

**Recursive strip**:

The `strip` parameter must be applied recursively, not just to the root node.
Embedded blank nodes from nested objects also carry `@id` (e.g. `"@id": "_:b0"`),
and consumers expecting plain JSON do not want those either. The implementation
must walk the entire output tree and remove listed keys from every object node.

```
-- Example: calibration sub-objects without recursive strip
{ "calibration": [ { "@id": "_:b0", "date": "2026-01-15" } ] }

-- With recursive strip (default behaviour)
{ "calibration": [ { "date": "2026-01-15" } ] }
```

Pass `strip => ARRAY[]::TEXT[]` to suppress recursive stripping entirely and
receive a full JSON-LD node graph, including embedded `@id`s — useful when
the consumer is itself a JSON-LD processor.

**`@container: @list` support (known gap — confirmed bug)**:

Arrays round-trip correctly only for *unordered* sets (e.g. tags). For ordered
arrays, JSON-LD 1.1 requires `@container: @list` in both the inbound context
and the outbound frame. This is **not currently implemented**; the gap exists
in two places:

1. **Inbound** (`src/bulk_load.rs`, `json_to_ntriples` context parser): context
   entries with object values — e.g. `{"@id": "ex:steps", "@container": "@list"}`
   — are silently dropped because the parser only matches `serde_json::Value::String`.
   The predicate IRI is never registered, and the array is stored as unordered
   parallel triples rather than an `rdf:List` cons-cell chain.

2. **Outbound** (`src/framing/embedder.rs`, `build_node_map`): object values are
   collected into `Vec<Value>` with no ordering guarantee. `@container: @list`
   in the frame is not read; `rdf:first` / `rdf:rest` traversal is absent.

This bug is fixed in v0.72.0 as **BUG-JSONLD-LIST-01** (see below).

**Implementation location**: `src/export/jsonld_node.rs` (new file). The existing `export_jsonld_framed` Rust implementation is called internally; no SPARQL translation logic is duplicated.

**Doc update**: Update `docs/src/operations/pg-trickle-relay.md` Step 5 trigger to use `export_jsonld_node()` once the function is implemented, replacing the current `export_jsonld_framed() || jsonb_build_object('@id', ...)` + unwrap + strip pattern.

**Tests**: Add to `tests/pg_regress/sql/export_jsonld_node.sql`:
- Returns correct plain JSON with default `strip`.
- Returns full JSON-LD node with `strip => ARRAY[]::TEXT[]`.
- Returns `NULL` when no triples match the subject.
- Errors with a descriptive message when `subject_id` is not in the dictionary.
- Frame that already contains `@id` raises an error (guard against accidental override).
- Nested object (blank node sub-object) has `@id` stripped recursively by default.
- Nested object retains `@id` when `strip => ARRAY[]::TEXT[]`.

---

### BUG-JSONLD-LIST-01 — `@container: @list` round-trip fidelity

**Root cause**: Two independent gaps prevent ordered arrays from round-tripping correctly through the ingest → triplestore → frame pipeline.

**Fix 1 — Inbound context parser** (`src/bulk_load.rs`, `json_to_ntriples`):

The context loop matches only `serde_json::Value::String` values. An entry like
`"steps": {"@id": "ex:steps", "@container": "@list"}` is an object, so it is
silently discarded and the predicate is never registered.

1. Extend the context loop to also match `serde_json::Value::Object`: extract the
   `@id` string as the predicate IRI and record `@container` in a separate
   `HashMap<String, String>` (key → container type).
2. In `json_object_to_ntriples_inner`, when the array branch fires and the
   predicate's container type is `"@list"`, emit an `rdf:List` cons-cell chain
   (`rdf:first` / `rdf:rest`) instead of parallel triples. Terminate with
   `rdf:nil`.

**Fix 2 — Outbound framing embedder** (`src/framing/embedder.rs`, `build_node_map` and frame matching):

1. When resolving a frame property slot, check if the compacted frame context
   declares `@container: @list` for that property.
2. If so, instead of collecting object values directly, traverse the stored
   `rdf:first` / `rdf:rest` chain rooted at the blank node value and collect
   objects in chain order.
3. Emit the collected values as a JSON array in the output node.

**Tests**: Add to `tests/pg_regress/sql/jsonld_list_roundtrip.sql`:
- Ingest a JSON array of strings with `@container: @list`; verify stored triples
  form an `rdf:List` chain (query `rdf:first` / `rdf:rest` directly).
- Frame the result with `@container: @list`; verify output array order matches input.
- Ingest a JSON array of objects with `@container: @list`; verify nested blank
  nodes are chained and round-trip in order.
- Ingest an array *without* `@container: @list`; verify it is stored as parallel
  triples (existing behaviour unchanged).

---

## Exit criteria

- [ ] XACT-01: SAVEPOINT rollback test passes; no phantom derived triples.
- [ ] CACHE-01: Plan cache docs created; role-isolation test passes.
- [ ] MOD-01: Three target files ≤ 500 lines each; no pg_regress regressions.
- [ ] PROPTEST-01: `cargo test --features pg18` includes proptest suite with ≥ 100 cases.
- [ ] FUZZ-02: 13th fuzz target present; seeds included.
- [ ] CONF-01: W3C syntax/basic/optional/graph subsets and BSBM are blocking CI steps.
- [ ] BENCH-03: History CSV has ≥ 4 rows; weekly workflow appends rows.
- [ ] FLIGHT-NONCE-01: Replay rejection HTTP test passes.
- [ ] CWB-DATALOG-01: Datalog → CWB interaction test passes.
- [ ] OBS-02: `/metrics/extension` route returns `streaming_metrics()` JSON.
- [ ] GUC-DOC-01: GUC reference doc created.
- [ ] CITUS-URL-01: URL parsing edge-case tests pass.
- [ ] JSONLD-NODE-01: `export_jsonld_node()` function exists; recursive strip works on nested blank nodes; pg_regress tests pass; `docs/src/operations/pg-trickle-relay.md` Step 5 updated to use the new function.
- [ ] BUG-JSONLD-LIST-01: `@container: @list` ingest emits `rdf:List` chain; framing embedder traverses chain in order; round-trip order test passes.
- [ ] All 190+ pg_regress tests pass.
