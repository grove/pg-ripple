# v0.86.0 — Assessment 13 Tests, API Polish, Observability & Security

> **Full technical details:** [v0.86.0-full.md](v0.86.0-full.md)

**Status: Planned** | **Scope: Large**

> v0.86.0 is the third and final Assessment 13 remediation release. It delivers test coverage (sparql_roundtrip proptest vs reference evaluator, CONSTRUCT + SHACL-SPARQL fuzz targets, conformance trend artifacts, benchmark regression gate, CDC crash-recovery test), API polish (OpenAPI YAML committed to CI, error-code registry, deprecated GUC docs, SPARQL error response standardisation), observability (federation/dict LRU cache Prometheus metrics, `explain_sparql()` algebra_after_opt field, structured JSON log output, axum graceful shutdown), supply chain (dependency upgrades, SBOM CI gate), and HTTP companion security (CORS blocked counter, file-path canonicalisation, Arrow 413 guard, auth scheme docs). See the migration script `sql/pg_ripple--0.85.0--0.86.0.sql` for the full list of changes.
