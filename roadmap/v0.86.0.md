# v0.86.0 — Assessment 13 Tests, API Polish, Observability, Supply Chain & Standards

> **Full technical details:** [v0.86.0-full.md](v0.86.0-full.md)

**Status: Released ✅** | **Scope: Large**

> v0.86.0 closes the remaining 30+ Low-priority and backlog findings from
> Assessment 13 (`plans/PLAN_OVERALL_ASSESSMENT_13.md`) spanning test coverage,
> API polish, documentation, supply chain, observability, standards conformance,
> HTTP companion, security hardening, and concurrency. After v0.86.0 all 82
> A13 findings are resolved and the codebase is ready for the v0.87.0
> Uncertain Knowledge Engine.

---

## Headline outcomes

### Tests (T13-02 – T13-07)
- [x] **T13-02** — `tests/proptest/sparql_roundtrip.rs` extended to compare
  result sets against spargebra's reference evaluator on a small in-memory
  graph (not just outcome-only invariants)
- [x] **T13-03** — two new fuzz targets: `fuzz/fuzz_targets/construct_rule.rs`
  and `fuzz/fuzz_targets/shacl_sparql.rs`; wired into weekly fuzz CI job
- [x] **T13-04** — CI artifact `tests/conformance/history.csv` tracks per-
  version pass rates across all five conformance suites (columns: version,
  suite, total, passed, failed, skipped); `docs/src/reference/conformance-
  trends.md` page plots the history
- [x] **T13-05** — `#[pg_extern]` coverage gap re-audited: `grep '#[pg_extern]'
  src/ | wc -l` vs functions referenced in regress tests; actual gap closed
- [x] **T13-06** — CI benchmark regression gate verified: `scripts/
  bench_check_regression.py --fail-on-regression 10` wired into the benchmark
  workflow
- [x] **T13-07** — crash-recovery test for CDC slot cleanup worker:
  `tests/crash_recovery/cdc_slot_cleanup_during_kill.sh` creates a slot,
  SIGKILLs the worker mid-cleanup, asserts slot is reclaimed on restart

### API polish (A13-01 – A13-06)
- [x] **A13-01** — `json_ld_load` alias comment updated to
  `-- DEPRECATED: removal scheduled for v1.0.0`
- [x] **A13-02** — `pg_ripple_http/openapi.yaml` committed to repo; CI step
  fails if in-binary OpenAPI does not match committed file
- [x] **A13-03** — `docs/src/reference/error-codes.md` created listing every
  PT code with meaning and source file; CI lint enforces PT codes on production
  error paths
- [x] **A13-04** — `docs/src/reference/deprecated-gucs.md` lists all deprecated
  GUCs with replacement names and removal versions
- [x] **A13-05** — regression test asserts `g BIGINT` column present in every
  VP-row-returning SQL function
- [x] **A13-06** — SPARQL parse errors standardised to `PT400_SPARQL_PARSE`
  error code across HTTP companion and extension

### Documentation (D13-01 – D13-05)
- [x] **D13-01** — `docs/src/operations/compatibility.md` updated with v0.80–
  v0.84 rows; aligned with new `COMPATIBLE_EXTENSION_MIN`
- [x] **D13-02** — reference docs added for `strict_dictionary` GUC, CDC slot
  cleanup, `plan_cache_capacity` GUC, `merge_worker_status` table
- [x] **D13-03** — `plans/sparql12_tracking.md` cross-checked against W3C
  SPARQL 1.2 community group draft; gaps noted and tracked
- [x] **D13-04** — `blog/README.md` index generated listing all 30+ posts in
  version order
- [x] **D13-05** — `plans/probabilistic-features.md` linked from ROADMAP.md
  v0.87.0 section header

### Supply chain (DS13-01 – DS13-05)
- [x] **DS13-01** — `cargo upgrade --dry-run` triaged: `parquet` → 60.x,
  `ureq` → 3.x, `arrow` → 56.x patch upgrades applied; `pgrx` and `oxrdf`
  pinned for ABI stability
- [x] **DS13-02** — `audit.toml` expiry dates added to all three RUSTSEC
  ignores; `RUSTSEC-2021-0127` removed if `serde_cbor` is absent from
  `Cargo.lock`
- [x] **DS13-03** — `.github/workflows/release.yml` verified to regenerate and
  commit SBOM on tag; CI failure added if SBOM version ≠ `Cargo.toml` version
- [x] **DS13-04** — `rust-toolchain.toml` Renovate/Dependabot auto-update
  configured in `renovate.json`
- [x] **DS13-05** — `tokio-stream` dependency resolved: either SSE streaming
  cursors implemented (`HTTP-02`) or the dependency removed

### Observability (O13-02 – O13-05)
- [x] **O13-02** — Prometheus metrics added: `federation_endpoint_duration_
  seconds{endpoint=…}`, `dictionary_cache_hit_ratio`,
  `merge_worker_delta_rows_pending`
- [x] **O13-03** — `explain_sparql()` output gains `algebra_optimised` field
  (post-sparopt algebra tree) and per-operator applied-transform annotations
- [x] **O13-04** — `pg_ripple_http` respects `RUST_LOG_FORMAT=json` to switch
  `tracing-subscriber` to JSON layer for structured log output
- [x] **O13-05** — `axum::serve(...).with_graceful_shutdown(shutdown_signal())`
  added; 30-second drain on SIGTERM

### Security (S13-03, S13-04, S13-06 – S13-10)
- [x] **S13-03** — `cors_permissive_requests_total` Prometheus counter added;
  `docs/src/operations/security.md` warns against `*` origin in production
- [x] **S13-04** — `audit.toml` expiry dates added (see DS13-02)
- [x] **S13-06** — file-path bulk loaders validate path against
  `data_directory` or `pg_ripple.bulk_load_dir` via `canonicalize()` + prefix
  check before opening
- [x] **S13-07** — `docker-compose.yml` uses `POSTGRES_PASSWORD_FILE` pattern
  (randomly generated secret at first run) instead of a hardcoded password
- [x] **S13-08** — Arrow Flight endpoint returns HTTP 413 with a generic
  message (no row count) before materialising oversized results; row count
  logged server-side only
- [x] **S13-09** — `pg_ripple_http` README top-level note warns operators to
  network-isolate the metrics port
- [x] **S13-10** — `docs/src/operations/security.md` documents supported auth
  schemes (Bearer only; Basic not accepted)

### Standards conformance (SC13-01 – SC13-04)
- [x] **SC13-01** — `pg_ripple.sparql_strict = on` verified to make unknown-
  function FILTER predicates an error; regression test per filter case added
- [x] **SC13-02** — `SERVICE SILENT` verified to swallow TLS errors, JSON parse
  errors, response-size-limit errors, and redirect loops; regression tests added
  for each case
- [x] **SC13-03** — `docs/src/reference/geosparql-functions.md` created with
  status table for all ~30 GeoSPARQL 1.1 functions
- [x] **SC13-04** — `docs/src/reference/sparql-compliance.md` documents
  DESCRIBE algorithm choices (CBD, SCBD, Symmetric CBD);
  `pg_ripple.describe_form` GUC added (values: `cbd`, `scbd`, `symmetric`)

### HTTP companion (HTTP-02, HTTP-03)
- [x] **HTTP-02** — SSE-based streaming SELECT results implemented in
  `pg_ripple_http/src/stream.rs`; `tokio-stream` dependency justified
- [x] **HTTP-03** — Arrow Flight verified to use `Body::from_stream` path;
  `max_export_rows` enforced before materialisation; regression test added

### Code quality (Q13-06 – Q13-08)
- [x] **Q13-06** — CI cap added: max 3 `unwrap`/`expect` per file unless a
  `// CLIPPY-OK: <reason>` comment immediately precedes
- [x] **Q13-07** — all 9 `unreachable!` calls in production code converted to
  `pgrx::error!("internal: <description> — please report")` at:
  `src/datalog/explain.rs:112`, `src/sparql/federation.rs:198`,
  `src/views.rs:839,869`, `src/construct_rules/mod.rs:239`,
  `src/construct_rules/delta.rs:111,138`, `src/replication.rs:78`
- [x] **Q13-08** — `cargo geiger` (or equivalent) added to CI; flag any
  increase in unsafe-block count of more than 10%

### Concurrency (CC13-03 – CC13-05)
- [x] **CC13-03** — CDC LSN watermark updated in batches (every 100 events or
  500 ms) using `INSERT ... ON CONFLICT (slot_name) DO UPDATE SET last_lsn =
  GREATEST(last_lsn, EXCLUDED.last_lsn)`; benchmark confirms less than 5%
  throughput overhead vs per-event update
- [x] **CC13-04** — intra-stratum cycle pre-check guards SCC evaluator (see
  P13-06 in v0.85.0)
- [x] **CC13-05** — `unreachable!` in BGP encoder paths replaced with
  `pgrx::error!` (see Q13-07)

### Developer experience (RR-03)
- [x] **RR-03** — line coverage badge added to `README.md` (`cargo llvm-cov
  --lcov` or `cargo tarpaulin`); CI step fails when measured line coverage
  drops below 80%; badge SVG committed and auto-updated on each merge to `main`
  (Assessment 13 recommended new roadmap item; see Area 5 / T13-04 rationale)
