# v0.84.0 — Assessment 13 Critical/High & Operational Remediation

> **Full technical details:** [v0.84.0-full.md](v0.84.0-full.md)

**Status: Released ✅** | **Scope: Large**

> v0.84.0 closes all Critical and High findings from Assessment 13
> (`plans/PLAN_OVERALL_ASSESSMENT_13.md`). The headline items are: HTTP
> companion version sync (6-version drift since v0.77.0), docker-compose image
> tag currency, SECURITY DEFINER inline documentation, CI SQL-injection-check
> gate, migration-chain coverage for v0.80–v0.83, gucs/registration.rs split
> (2,032 lines), /health/ready deep-check, plan cache double-parse elimination,
> justfile automation recipes, and the Assessment #13 process findings
> (PROMPT-01 assessment-scope rule; V084-01 uncertain knowledge deferred to
> v0.87.0).

---

## Headline outcomes

- [x] **MF-B / HTTP-01** — bump `pg_ripple_http` to `0.84.0`; raise
  `COMPATIBLE_EXTENSION_MIN` from `"0.79.0"` to `"0.84.0"` in
  `pg_ripple_http/src/main.rs:38`
- [x] **S13-05** — `PG_RIPPLE_HTTP_STRICT_COMPAT=1` env var converts the
  `COMPATIBLE_EXTENSION_MIN` version mismatch warning into a startup error;
  default off for backward compatibility
- [x] **BUILD-01** — `docker-compose.yml` image tag bumped from `0.54.0` to
  `0.84.0`; CI lint step added to `ci.yml` asserting that the tag matches
  `Cargo.toml` version
- [x] **S13-01** — both `SECURITY DEFINER` occurrences (`src/schema.rs:996` and
  `sql/pg_ripple--0.55.0--0.56.0.sql:60`) annotated with inline
  `-- SECURITY-JUSTIFY: <reason>` comment; `check_no_security_definer.sh`
  updated to require the marker
- [x] **S13-02** — `scripts/check_no_string_format_in_sql.sh` wired as a
  required CI step in `.github/workflows/ci.yml`; all 126 `format!()` SQL
  patterns verified against the safe allowlist (numeric pred_id/graph_id only)
- [x] **T13-01** — `tests/test_migration_chain.sh` extended with checkpoint
  assertions for v0.80.0 (CDC slot tables), v0.81.0 (strict_dictionary GUC,
  dict_subxact tracking), v0.82.0 (`_pg_ripple.merge_worker_status` table),
  v0.83.0 (new tables from that release) — approximately 30 lines of bash
- [x] **Q13-01** — `src/gucs/registration.rs` (2,032 lines) split into
  per-domain submodules: `src/gucs/registration/sparql.rs`,
  `storage.rs`, `federation.rs`, `datalog.rs`, `security.rs`,
  `observability.rs`; public re-exports from `src/gucs/registration.rs`
  unchanged so callers are unaffected
- [x] **C13-01** — nested `OPTIONAL` + `EXISTS` filter-pushdown regression test
  added in `tests/pg_regress/sql/sparql_optional.sql`; root cause investigated
  and fixed if test exposes a failure
- [x] **O13-01** — `pg_ripple_http` `/health/ready` endpoint added; performs a
  real PostgreSQL round-trip (`SELECT 1 FROM pg_extension WHERE
  extname='pg_ripple'`) with a 2-second timeout; returns `503` on failure;
  `/health` remains a fast liveness probe with no DB call
- [x] **P13-01** — `cache_key()` in `src/sparql/plan_cache.rs` refactored to
  accept a pre-parsed `&spargebra::Query`; callers in `execute.rs` and
  `plan.rs` updated to parse once and pass the parsed query through; eliminates
  the double-parse on every cache-miss path
- [x] **BUILD-02** — `justfile` gains four new recipes: `bump-version
  NEW_VERSION` (updates Cargo.toml, pg_ripple_http/Cargo.toml,
  pg_ripple.control, COMPATIBLE_EXTENSION_MIN, docker-compose tag, creates
  migration script stub, appends CHANGELOG header), `regen-sbom`,
  `regen-openapi`, `check-version-sync`
- [x] **BUILD-03** — migration chain test verified as a required step in
  `ci.yml`; added if not present

### Process & scope (PROMPT-01, V084-01)
- [x] **PROMPT-01** — `plans/overall_assesment_prompt.md` template updated to
  anchor future assessment scope at the latest *tagged* release; adds note:
  "scope = `git describe --tags --abbrev=0`" so auditors are never evaluating
  undelivered planned versions
- [x] **V084-01** — v0.84.0 scope decision recorded in `ROADMAP.md`: uncertain
  knowledge engine (probabilistic Datalog, fuzzy SPARQL, soft SHACL, PROV
  confidence) renamed to **v0.87.0**; v0.84.0–v0.86.0 reserved for Assessment
  13 remediation; decision rationale archived in v0.84.0-full.md Area 9
