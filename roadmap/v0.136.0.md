# v0.136.0 - External audit readiness and hardened candidate

**Status:** Candidate implementation complete; external audit pending

**Goal:** Prepare a reproducible v0.135.0-compatible candidate for the
independent audit required before v1.0.0.

## Delivered

- [x] Propagate the v0.136.0 version through the extension, HTTP companion,
  OpenAPI document, images, Compose, Helm, SBOM, CI evidence, and stable API
  manifest.
- [x] Add the `0.135.0 -> 0.136.0` migration and fresh-install schema stamp.
- [x] Correct the extension's HTTP companion minimum to `0.135.0`, matching
  the frozen v1 query contract.
- [x] Add the audit engagement brief, finding ledger, and static audit
  preflight gate.
- [x] Add regression coverage for the version and schema ledger invariants.
- [x] Keep the v0.135.0 public SQL and HTTP API unchanged.

## Verification

```text
bash scripts/check_v0136_audit.sh
bash scripts/check_v0134_streaming.sh
bash scripts/check_migration_headers.sh
python3 scripts/check_http_routes.py --strict
python3 scripts/migration_graph.py
```

## External gate

The independent audit report, remediation confirmation, and zero-open-
Critical-or-High attestation are not generated by this repository. The
finding ledger remains explicitly pending until the auditor supplies them.

## Exit criteria

- The candidate has one versioned migration path from v0.135.0.
- Local security, route, migration, streaming, binding, and prefix checks pass.
- The external auditor receives the exact candidate commit and artifacts.
- The final report and remediation confirmation are attached before the
  v0.136.0 release is tagged.
