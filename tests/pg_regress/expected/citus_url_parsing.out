-- citus_url_parsing.sql
-- CITUS-URL-01 (v0.72.0): Edge-case URL parsing tests.
--
-- The primary coverage is via Rust unit tests in src/citus.rs.
-- This pg_regress file verifies that the pg_ripple extension loads without
-- errors and that Citus-related introspection functions are callable.
SET client_min_messages = warning;
CREATE EXTENSION IF NOT EXISTS pg_ripple;
SET client_min_messages = DEFAULT;
SET search_path TO pg_ripple, public;
-- Verify that citus_available() is callable (returns boolean, no panic).
SELECT pg_ripple.citus_available() AS citus_available;
 citus_available 
-----------------
 f
(1 row)

-- Verify feature_status() includes citus entry (smoke test for citus module).
SELECT COUNT(*) > 0 AS citus_in_feature_status
FROM   pg_ripple.feature_status()
WHERE  feature_name LIKE 'citus%';
 citus_in_feature_status 
-------------------------
 t
(1 row)

-- URL parsing unit tests run as part-- URL parsing unit tests run as part-- URL parsing unit tests run as part-- URL parsing unit tests run th-- URL parsing unit tesal-- URL parsing unit tests run as part-- URL parsing un1.int-- URL parsing unit tests run as part-- URL parsing unit tests â-- URL parsing unit tests run as part-- URL parsing unit tests run as part xn--bcher-kva.example.com
--   - Port-only, no path: http://host:5432   --   - Port-only, no path: http:/med URL:     not-a-url                      â†’ "" (empty, not a panic)
-- All -- All -- All -- All -- All -- All ::u-- All -- All -- All -- All -- All -- All ::u-- All -- All -- All -- All s-- All -- All -- All -- All -- All -- All ::u-- All -  -- All -- All - co-- All -- All -      -- A       
------------------------------------------------------------
 CITUS-URL-01 u CITUS-URL-01 u CITUS-URL-01 u CITUS-URL-01 
(1 row)

