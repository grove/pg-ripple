# Unified conformance known-failures list
#
# Format: <suite>:<test-key> issue=<URL> owner=<team/person>
#         rationale=<text> expires=<YYYY-MM-DD>
#
# Suite prefixes:
#   w3c:     W3C SPARQL 1.1 test suite (test IRI)
#   jena:    Apache Jena test suite (test IRI)
#   watdiv:  WatDiv benchmark template (template ID, e.g. S1, B7)
#
# A test listed here is reported as XFAIL. CI rejects missing fields and expired
# entries; an XFAIL that unexpectedly passes is an XPASS and fails required jobs.
#
# Guidelines:
#   - Only add entries for confirmed bugs, not framework issues.
#   - Add a short explanation or ticket link.
#   - Remove entries as soon as the underlying bug is fixed.
#
# ─────────────────────────────────────────────────────────────────────────────
# W3C entries (v0.41.0+): 0 known failures as of v0.43.0
# ─────────────────────────────────────────────────────────────────────────────

# ─────────────────────────────────────────────────────────────────────────────
# Jena entries (v0.43.0+): ≤ 30 entries expected for type-coercion edge cases
# ─────────────────────────────────────────────────────────────────────────────

# spargebra limitation: longest-token rule for <?a&&?b> not enforced by the parser
# Reason: spargebra 0.4.x does not implement the SPARQL longest-token rule for
#   the &&-adjacent-to-IRI-literal edge case in variable expressions. The test
#   requires the parser to reject "<?a&&?b>" as a syntax error, but spargebra
#   accepts it. This is a parser library limitation; no local workaround exists.
# Issue: https://github.com/oxigraph/spargebra/issues/123
jena:syn-bad-28.rq issue=https://github.com/oxigraph/spargebra/issues/123 owner=pg-ripple rationale=spargebra does not enforce the SPARQL longest-token rule expires=2026-12-31

# ─────────────────────────────────────────────────────────────────────────────
# WatDiv entries (v0.43.0+): none expected for correctness failures
# ─────────────────────────────────────────────────────────────────────────────

# ─────────────────────────────────────────────────────────────────────────────
# LUBM entries (v0.44.0+): 0 known failures at release
# ─────────────────────────────────────────────────────────────────────────────
