# v0.136.0 audit findings ledger

Status: awaiting the independent external audit.

This ledger records the audit scope and disposition format. It does not claim
that an external audit has completed. Findings must be added here with a
severity, owner, remediation reference, regression test, and disposition as
the auditor reports them.

| Area | Status | Evidence or owner |
|---|---|---|
| PostgreSQL and pgrx boundary | pending external audit | `audit/engagement-brief.md` |
| unsafe Rust, hooks, shared memory, latches, workers | pending external audit | Rust maintainers |
| dynamic SQL and identifier handling | local checks pass; audit pending | `scripts/check_no_string_format_in_sql.sh` |
| SECURITY DEFINER, grants, ownership, RLS, search path | local checks pass; audit pending | `scripts/check_security_definer_search_path.sh` |
| dictionary, journal, merge, promotion, tombstones | pending external audit | Extension maintainers |
| JSON writeback and asynchronous pipelines | pending external audit | `tests/pg_regress/sql/v0128_json_writeback.sql` |
| HTTP authentication and authorization | local checks pass; audit pending | `scripts/check_http_routes.py` |
| SSRF, DNS rebinding, redirects, credentials, egress | local checks pass; audit pending | `results/federation_ssrf_policy.out` |
| parser and query resource exhaustion | local fuzz targets retained; audit pending | `fuzz/fuzz_targets/` |
| HTTP streaming lifecycle and output encoding | local qualification pass; audit pending | `scripts/check_v0134_streaming.sh` |
| typed bindings and generated SQL parameters | local regression pass; audit pending | `tests/pg_regress/sql/v0135_bindings.sql` |
| prefix privileges and transactional cache invalidation | local regression pass; audit pending | `tests/pg_regress/sql/v0135_prefix_mode.sql` |
| Docker, Compose, Helm, supply chain, migrations | local release checks pass; audit pending | `scripts/build_release_evidence.py` |

## Finding record

Use this shape for each finding received from the auditor:

| ID | Severity | Owner | Remediation PR | Regression test | Disposition |
|---|---|---|---|---|---|
| none received | n/a | n/a | n/a | n/a | Awaiting auditor report |

The release cannot claim zero unresolved Critical or High findings until the
external report and remediation confirmation are attached to the release
evidence bundle.
